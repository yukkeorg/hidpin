# hidpin

RP2040系マイコンボードのGPIO入力を監視し、その**ピンレベル**をUSB-HIDで**ホスト**に伝える仕組み。構想段階の仮称は「RPI204x-IO」。

## Language

### 構成要素

**デバイス**:
GPIOの監視と駆動を行い、HIDとして**ホスト**に接続されたマイコンボード（とその上のファームウェア）。
_Avoid_: ボード、ターゲット、マイコン

**ホスト**:
**デバイス**がUSBで接続され、監視結果を受け取るコンピュータ。1台の**ホスト**に複数の**デバイス**が接続されうる。
_Avoid_: PC、コンピュータ、クライアント

**デバイス情報**:
**デバイス**が自身について**ホスト**に申告する事実の組。プロトコル版、ファームウェア版、ボード種別、**利用可能GPIO**などからなる。**ホスト**は**ピン設定**を組み立てる前にこれを読む。
_Avoid_: ステータス、プロファイル

**シリアル番号**:
**デバイス**の個体を識別する、ボード固有で書き換えられない文字列。**ホスト**は複数の**デバイス**をこれで見分ける。
_Avoid_: ID、名前、デバイス番号

### GPIO

**利用可能GPIO**:
ボードの端子として外に出ており、ボード上の部品（LED等）に接続されていないGPIO。どれが該当するかはボードごとに決まる。
_Avoid_: 全ピン、空きピン

**監視ピン**:
**利用可能GPIO**のうち、**ピンレベル**を**ホスト**へ伝える対象として有効にされたもの。1台の**デバイス**に0本以上ある。
_Avoid_: 入力ピン、チャンネル、ポート

**出力ピン**:
**利用可能GPIO**のうち、**デバイス**が**出力レベル**を駆動する対象として有効にされたもの。1台の**デバイス**に0本以上ある。**監視ピン**と同時にはならない。
_Avoid_: 出力ポート、ドライブピン

**出力レベル**:
**出力ピン**が駆動する電気的な値（HIGH/LOW）。**ホスト**が指定する。
_Avoid_: 出力値、状態

**初期出力レベル**:
**ピン設定**で**出力ピン**ごとに指定する、安全側の**出力レベル**。GPIOが新たに**出力ピン**になったとき（**初期出力レベル**だけが変わったときを含む）と、USB の切断・サスペンド・バスリセット時に、**出力ピン**はこれに戻る。
_Avoid_: デフォルト出力、リセット値

**既定ピン設定**:
**デバイス**が起動したときに使う**ピン設定**。全**利用可能GPIO**を**監視ピン**とし、プルアップと既定の**チャタリング除去時間**を用いる。**出力ピン**は含まない。**ホスト**が変更した**ピン設定**は保存されず、再起動でこれに戻る。
_Avoid_: 初期設定、デフォルト

**ピン設定**:
**利用可能GPIO**ごとの指定で、「使わない」「監視する（プルアップ／プルダウン／なし、**チャタリング除去時間**を伴う）」「出力する（**初期出力レベル**を伴う）」のいずれか。**ホスト**が**デバイス**に対して指定する。使わないGPIOに**デバイス**は電気的に関与しない。
_Avoid_: コンフィグ、モード

**目標ピン設定**:
**ホスト**がある**デバイス**について保ち続けたい**ピン設定**。全**利用可能GPIO**について定め、指定のないGPIOは「使わない」とする。**デバイス**上の**ピン設定**がこれと食い違ったとき（再起動で**既定ピン設定**に戻った、別のプログラムが書き換えた）は、**ホスト**が書き直して合わせる。**出力レベル**は含まない。
_Avoid_: 望むピン設定、希望設定

**チャタリング除去時間**:
GPIOの電気的な値が変わってから、変わらずに続いたときに初めて**ピンレベル**の変化として認める時間。0は除去しないことを表す。
_Avoid_: デバウンス、フィルタ時間

**ピンレベル**:
**監視ピン**の電気的な値（HIGH/LOW）を**チャタリング除去時間**で確定させたもの。**デバイス**が知っているのはこれだけで、配線上の意味は持たない。
_Avoid_: 状態、値、ON/OFF

**極性**:
ある**監視ピン**について「HIGHとLOWのどちらを**ON**とみなすか」の指定。**ホスト**側で持ち、**デバイス**は関知しない。明示されない場合は**ピン設定**のプルから決まる（プルアップならLOWがON、それ以外はHIGHがON）。
_Avoid_: 反転、アクティブロー

**ON / OFF**:
**ピンレベル**を**極性**で解釈した、配線上の意味（スイッチが入っているか等）。**ホスト**側でのみ現れる。
_Avoid_: HIGH/LOW（それは**ピンレベル**）、真偽

### 通知

**状態通知**:
**デバイス**が全**監視ピン**の**ピンレベル**と全**出力ピン**の**出力レベル**を一度に**ホスト**へ伝えるもの。いずれかの**監視ピン**の**ピンレベル**が変化したとき、**出力レベル**が変更されたとき、**ピン設定**が変更されたとき、変化がなくても一定間隔で、および**ホスト**が要求したときに発生する。直前の**状態通知**以降に確定した**エッジイベント**を0個以上含み、それらを順に当てはめた結果がその**状態通知**の**ピンレベル**と一致する。**デバイス**が保持しきれずに捨てた**エッジイベント**があれば、そのことを示す。
_Avoid_: イベント、割り込み、データ

**エッジイベント**:
ある**監視ピン**の**ピンレベル**が変化した1回の出来事。対象のGPIO、変化後の**ピンレベル**、変化が始まった時刻（確定した時刻ではない）からなる。チャタリングによる一時的な揺れはこれに含まれない。
_Avoid_: エッジ、割り込み、イベント（単独）

**ON/OFF 変化**:
ある**監視ピン**の **ON / OFF** が変わったこと。**ホスト**側でのみ現れる。通常は**エッジイベント**を**極性**で解釈して得るので、変化が始まった時刻を持つ。**エッジイベント**が失われた場合（**ホスト**の読み落とし、**デバイス**が捨てたもの、切断中の変化）は、**状態通知**の**ピンレベル**と直前に把握していた **ON / OFF** の食い違いから補う。補ったものは時刻を持たない。直前の **ON / OFF** を把握していない**監視ピン**（接続して初めて知ったもの、新たに**監視ピン**になったもの）について最初に知った値は、**ON/OFF 変化**ではない。
_Avoid_: 入力イベント、切り替わり、エッジ

## Example dialogue

> **開発者**: スイッチを押したのに、**状態通知**では GPIO5 が 0 になっています。壊れていますか？
> **ドメインエキスパート**: 正常です。**既定ピン設定**はプルアップなので、押すと**ピンレベル**は LOW になります。**デバイス**が伝えるのは**ピンレベル**だけです。
> **開発者**: では「押された」と判断するのはどこですか？
> **ドメインエキスパート**: **ホスト**です。GPIO5 の**極性**は明示していなければプルアップから「LOW が **ON**」と決まるので、**ホスト**側では **ON** と表示されます。
> **開発者**: 押した瞬間の時刻は、**状態通知**の時刻を見ればいいですか？
> **ドメインエキスパート**: **エッジイベント**の時刻を見てください。**状態通知**が作られた時刻ではなく、変化が始まった時刻です。**チャタリング除去時間**の分だけ、**状態通知**より前になります。
> **開発者**: GPIO5 を「使わない」にしたら、プルアップは残りますか？
> **ドメインエキスパート**: 残りません。使わないGPIOに、**デバイス**は電気的に関与しません。
> **開発者**: GPIO7 をランプ用の**出力ピン**にして HIGH にしたまま PC がスリープしたら？
> **ドメインエキスパート**: USB がサスペンドした時点で、GPIO7 は**初期出力レベル**に戻ります。だから**初期出力レベル**には、ランプが消える側を指定しておきます。
> **開発者**: 同じ**ホスト**に2台つないだ場合、どちらの**状態通知**かはどう区別しますか？
> **ドメインエキスパート**: **シリアル番号**で区別します。ボードの種類と使えるGPIOは、**デバイス情報**を読めばわかります。

## Flagged ambiguities

- 「状態」は**ピンレベル**（HIGH/LOW）、**出力レベル**、**ON / OFF**のいずれにも読めるため、単独では使わない。**デバイス**が送るのは常に**ピンレベル**と**出力レベル**。
- 「イベント」は**エッジイベント**と**状態通知**のどちらにも読めるため、単独では使わない。
